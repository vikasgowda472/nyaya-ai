# Keep provider credentials on the server. The service works in retrieval-only mode when unset.
NYAYA_MODEL_PROVIDER=disabled
NYAYA_MODEL_NAME=
OPENAI_API_KEY=
GEMINI_API_KEY=
NYAYA_CORS_ORIGINS=http://localhost:3000
